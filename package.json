{
  "name": "nimbusguard-automation",
  "version": "1.0.0",
  "description": "Full-stack Lead Generation & Marketing Automation App",
  "main": "server/server.js",
  "scripts": {
    "install:client": "npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "build": "npm run install:client && npm run build:client",
    "start": "node server/server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
